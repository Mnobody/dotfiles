#!/bin/bash

# alias
alias nv='nvim .'
alias l='ls -l'

# tmux sessionizer
export PATH="$HOME/.local/bin:$PATH"
bind -x '"\C-f":tmux-sessionizer'

# starship.rs
eval "$(starship init bash)"

# in .bashrc:   source $HOME/.bashrc-extra
